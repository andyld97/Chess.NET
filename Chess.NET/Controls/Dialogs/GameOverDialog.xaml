<Window x:Class="Chess.NET.Controls.Dialogs.GameOverDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chess.NET.Controls.Dialogs"
        mc:Ignorable="d" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        xmlns:res="clr-namespace:Chess.NET.Properties"
        Title="{x:Static res:Resources.strGameOver}" Height="156" Width="630">
    <Grid Margin="10">
        <Grid x:Name="GridWin" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image x:Name="ImgKing" Source="/resources/icons/white/King.png" Height="96" Margin="10,0,0,0" />
            <StackPanel Grid.Column="1">
                <TextBlock HorizontalAlignment="Center" FontSize="22" Margin="0,10,0,0">                   
                    <Run x:Name="TextPlayer" Text="Player" FontWeight="Bold" />
                    <Run Text="{x:Static res:Resources.strPlayerWinsWith}" />
                    <Run x:Name="TextColor" Text="White" FontWeight="Bold" />
                    <Run Text="{x:Static res:Resources.strPlayerWinsDueTo}" />
                </TextBlock>

                <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="22" Foreground="#FF535353">
                    <Run x:Name="TextWinReason" Text="checkmate!" FontWeight="Bold" />
                </TextBlock>
            </StackPanel>
        </Grid>


        <Grid x:Name="GridDraw">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Image x:Name="ImgKingWhite" Source="/resources/icons/white/King.png" Height="96" Margin="10,0,0,0" />
                <Image x:Name="ImgKingBlack" Source="/resources/icons/black/King.png" Height="96" Margin="10,0,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock HorizontalAlignment="Center" FontSize="22" Margin="0,10,0,0">                   
                    <Run Text="{x:Static res:Resources.strDraw}" FontWeight="Bold" />
                    <Run Text="{x:Static res:Resources.strPlayerWinsDueTo}" />
                </TextBlock>

                <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="22" Foreground="#FF535353">
                    <Run x:Name="TextDrawReason" Text="Stalemate!" FontWeight="Bold" />
                </TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</Window>